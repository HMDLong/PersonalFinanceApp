import 'package:flutter/material.dart';

class NoticationBox extends StatelessWidget {
  const NoticationBox({super.key});

  @override
  Widget build(BuildContext context) {
    return ExpansionTile(title: Text("Noti"));
  }
}